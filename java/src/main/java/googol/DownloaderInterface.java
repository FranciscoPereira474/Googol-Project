package googol;

import java.rmi.*;

/**
 * Interface DownloaderInterface que define os métodos remotos para o downloader.
 * Esta interface é utilizada para comunicação entre o downloader e a gateway, 
 * utilizando RMI (Remote Method Invocation).
 */
public interface DownloaderInterface extends Remote{
        
}
